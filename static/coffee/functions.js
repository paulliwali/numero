// Generated by CoffeeScript 1.8.0
var addConditionInViewableBox, bindPlayerControls, randomNum, randomise, showError, showGameWin, showMessage, startGameMessage;

randomNum = function(max, min) {
  if (min == null) {
    min = 0;
  }
  return Math.floor(Math.random() * (max - min) + min);
};

randomise = function() {
  var randomNumAlt;
  randomNum = this._randomNum(10);
  return randomNumAlt = this._randomNum(10, 5);
};

showGameWin = function() {
  return swal({
    title: "You've Won!",
    type: "success"
  });
};

showMessage = function(message) {
  return swal({
    title: "Yes!",
    text: message,
    type: "success"
  });
};

showError = function(error) {
  return swal({
    title: "Oops!",
    text: error,
    type: "failure"
  });
};

$.fn.goto = function() {
  $("html, body").animate({
    scrollTop: $(this).offset().top + "px"
  }, "slow");
  this;
};

startGameMessage = function() {
  return swal({
    title: "Numero!",
    text: "Do you wanna play?",
    type: "info",
    showCancelButton: true,
    confirmButtonColor: "#5CB85C",
    confirmButtonText: "Let's Play!",
    cancelButtonText: "I don't want to play QQ",
    closeOnConfirm: true,
    closeOnCancel: false
  }, function(isConfirm) {
    if (isConfirm) {
      $("#game").goto();
      return $("#gameOptions").modal("show");
    }
  });
};

addConditionInViewableBox = function(condition) {
  return ELEMENT_CONDITIONS_CONTAINER.append(condition);
};

bindPlayerControls = function(playerNumber, e) {
  if (playerNumber === 1) {
    switch (e.keyCode) {
      case 68:
        return Game.prototype.dice.moveRight();
      case 83:
        return Game.prototype.dice.moveDown();
      case 65:
        return Game.prototype.dice.moveLeft();
      case 87:
        return Game.prototype.dice.moveUp();
    }
  } else if (playerNumber === 2) {
    switch (e.keyCode) {
      case 39:
        return Game.prototype.dice.moveRight();
      case 40:
        return Game.prototype.dice.moveDown();
      case 37:
        return Game.prototype.dice.moveLeft();
      case 38:
        return Game.prototype.dice.moveUp();
    }
  }
};
